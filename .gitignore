# JetBrains IDE settings — machine-specific, not part of the site
.idea/

# Source masters: the full-resolution originals the portrait derivatives and
# the OG card are generated from. Not served, so they don't belong in a deploy.
# Regenerate the derivatives with the crop/treatment script when one changes.
assets/portrait-source.png

# OS and editor artefacts
.DS_Store
Thumbs.db
*.swp
*~
